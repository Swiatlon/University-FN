import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StyledBox = styled(Box)(() => ({
  background: 'linear-gradient(180deg,#020b2c 50%, rgba(6, 14, 44, 1) 120%)',
  boxShadow: '0 8px 16px 0 rgba(0, 0, 0, 0.2);',
}));
